<h1>Listing books</h1>
<h2>Accounting</h2>
  <% if !@accounting.nil? %>
    <% @accounting.each do |f| %>
      <ul>
        <li><h3><%=link_to f.number, accounting_path(f.number)%></h3></li>
      </ul>
    <% end %>
  <% end %>
<h2>Hisotry</h2>
<h2>Music</h2>

<table>
  <thead>
    <tr>
      <th>Course</th>
      <th>Number</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.course %></td>
        <td><%= book.number %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path %>
